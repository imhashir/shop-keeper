<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android">

    <group
        android:checkableBehavior="single">
        <item
            android:title="@string/home_nav"
            android:id="@+id/home_nav"
            android:icon="@drawable/ic_home_menu"
            android:checked="true"/>
    </group>

    <item
        android:title="@string/sections_nav">
        <menu>
            <group
                android:checkableBehavior="single">
                <item
                    android:title="@string/customer_list"
                    android:icon="@drawable/customers_list_icon"
                    android:id="@+id/customer_list_nav"/>
            </group>
        </menu>
    </item>

</menu>